<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PlantMonitorApp" parent="android:Theme.Material.Light.NoActionBar">

        <!-- Splash screen customization -->
        <item name="android:windowSplashScreenBackground">@color/logo_image_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/logo_image_round</item>
        <item name="android:windowSplashScreenAnimationDuration">2000</item> <!-- ms -->
    </style>
</resources>